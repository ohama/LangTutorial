// Test: complex condition with && (CTRL-03 + CTRL-04)
// --- Command: dotnet run --project FunLang -- %input
// --- Input:
let x = 10 in
let y = 20 in
if x = 10 && y = 20 then 1 else 0
// --- Output:
1
